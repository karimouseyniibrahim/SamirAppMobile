<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.pfe.samir.HomeActivity"
    android:orientation="vertical">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="@dimen/mangin10"
        android:layout_marginTop="@dimen/mangin10"
        android:orientation="vertical"
        >
            <ImageView
                 android:layout_width="150dp"
                android:layout_height="150dp"
                android:src="@drawable/logo"
                />
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginTop="@dimen/_40dp"
        >
        <Button
            android:id="@+id/scanner"
            style="@style/scanner_button"
            android:text="Scanner"
             android:textSize="@dimen/_20dp"
            tools:bootstrapBrand="regular"
            android:onClick="scanner"
            />
        <Button
            android:id="@+id/historique"
            style="@style/scanner_button"
            android:text="Historique"
            android:textSize="@dimen/_20dp"
            tools:bootstrapBrand="regular"
            android:onClick="historique"
            />
        <Button
            android:id="@+id/Ecriture"
            style="@style/scanner_button"
            android:text="Ecriture"
            android:textSize="@dimen/_20dp"
            tools:bootstrapBrand="regular"
            android:onClick="ecriture"
            />
    </LinearLayout>

</LinearLayout>
